spring:
  application:
    name: domus

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: ${JWT_SECRET}
  expiration: ${JWT_EXPIRATION:3600000}

logging:
  level:
    org.hibernate: DEBUG
    org.hibernate.type: TRACE

app:
  demo-mode: true
  users:
    demo:
      name: Conta Demo
      email: demo@domus.app
      password: demo123
    admin:
      name: Igor Augusto
      email: igoraugusto146@gmail.com
      password: Stepflish123